#
# Browser client example environment.
# Copy this file to frontend/.env for local trader UI development.
# Do not commit real browser-facing credentials.
#

# Required by frontend/src/App.tsx.
# The UI currently uses same-origin API routing, so only the API key is needed here.
VITE_API_KEY=replace-with-the-configured-api-key
