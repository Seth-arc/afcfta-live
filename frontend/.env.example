#
# Browser client example environment.
# Copy this file to frontend/.env for local trader UI development.
# Do not commit real browser-facing settings.
#
# The trader UI now uses the same-origin /web/api route family.
# No browser-visible API key is required for local development.
# Keep this file for future non-secret browser config only.
