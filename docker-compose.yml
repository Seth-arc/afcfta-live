services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: afcfta
      POSTGRES_USER: afcfta
      POSTGRES_PASSWORD: afcfta_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

